import React from 'react';
import Header from '../Header/Header';

const Campaign = () => {
    return (
        <div>
            <Header></Header>
            <h1>Campaign here</h1>
        </div>
    );
};

export default Campaign;